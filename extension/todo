[X] export svg
[X] add objectified fact types
[X] add objectified label editing
[] add objectified connections to roles
[X] fancy directions for facts
[X] add derivable flag
[X] add unit-based reference mode
[X] keyboard support

[] actually write to file changes as they happen
[] hook up to element.changed or elements.change 
    - update the document
    - reyaml document and send back to vscode to add to file
[] grammar? trying with yaml
        - is it xml? or is just the export svg enough?
        - just use a basic toml or yaml setup?
[X] get updates from a file
[] add validation about what comes from the file updates
[] handle updates gracefully
[] load file content as first step on webview launch
[] add verb conventions to bottom of html?